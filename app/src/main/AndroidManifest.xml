<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pointnexus.heroes.heroestest">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".IntroActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".InserirActivity" />
        <activity android:name=".MagiasActivity" />
        <activity android:name=".ClassesActivity" />
        <activity android:name=".HeroiActivity" />
        <activity android:name=".AtualizarActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".FotosActivity"></activity>
    </application>

</manifest>